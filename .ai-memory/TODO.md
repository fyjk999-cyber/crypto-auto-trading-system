# TODO

- Restore postgres-parameterized test fixture + un-bare-skip 4 engine-loop
  integration tests (docs/POSTGRES_TEST_RESTORE_AUDIT.md restoration plan).
- Wire factor-profile BLOCKED verdict into execution authority decision path
  (integration decision, protected-core adjacent).
- Provision staging PostgreSQL + Docker.
- Run 24h+ paper soak and real boundary validation.
- Runtime failure injection and promotion/rollback dry-runs.
- Implement candidate validation pipeline + Safe Promotion (out of scope of the
  contract workstream by design).
