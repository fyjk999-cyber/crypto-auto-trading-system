# FACTOR API

- GET /api/factors/{symbol}          -> latest snapshot (full)
- GET /api/factors/{symbol}/history?factor=trend&limit=100 -> factor history
- GET /api/factors/{symbol}/snapshot -> latest snapshot (market state)
