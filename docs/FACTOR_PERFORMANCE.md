# FACTOR PERFORMANCE

FactorPerformanceTracker records TradeObservation(factor, value, pnl, result) and
computes sample_size, win_rate, average_return, sharpe, max_drawdown,
profit_factor per factor/symbol/timeframe.
Persisted in factor_performance table.
