# Deployment

Docker Compose production profile: non-root containers, healthchecks, restart
policies, resource limits, graceful shutdown. PostgreSQL not publicly exposed;
API behind reverse proxy; minimum exposed ports; SSH restricted.
