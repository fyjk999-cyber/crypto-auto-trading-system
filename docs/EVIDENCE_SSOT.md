# EVIDENCE SSOT

- Ledger = money truth
- DecisionEvidence = decision truth (factor_snapshot_id, factor_set_version, factor_profile)
- FactorSnapshot = factor-observation truth (append-only, immutable)
- Audit = operational truth
- Replay uses historical snapshots, never recomputes.
