# G00 — Runtime identity, source inventory and zero-Review root cause

Status: **READ-ONLY DIAGNOSIS COMPLETE** for the snapshots named below.
Data classification: engineering evidence only; no production write, no
migration approval and no claim that the learning loop is running.

## 1. Baseline and worktree isolation

| Item | Value |
| --- | --- |
| Task instruction said fullmarket HEAD | `98e4b6cb00d58af95e2f7ec34db7db4032375ba8` |
| Actual fullmarket HEAD at diagnosis | `df55b11bcde5d50670ef92d05ba2166feb4735d9` (`codex/full-market-factor-layer`) |
| Relationship | `98e4b6c` is an **ancestor** of `df55b11`; the only delta is `docs/ai-native-remediation/HANDOFF_2026-09-11.md` (2 insertions, 1 deletion) |
| Baseline re-bound by this task | `df55b11` (stale `98e4b6c` was not used as the implementation baseline) |
| This task worktree | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-growth` |
| This task branch | `codex/growth-learning-pipeline` (created from `df55b11`) |
| fullmarket checkout | untouched: still `df55b11` on `codex/full-market-factor-layer` |

`canonical-clean` was confirmed at detached `a548db928cd9c063ff33eb2a69c864f47f9ac64a`
(one dirty file: `data/paper-runtime.log`) — consistent with the task
statement that the worktree SHA alone proves neither the running process SHA
nor database ownership.

Pre-existing dirty/untracked files in fullmarket were **not copied, staged,
committed or reverted**. They belong to the other Harness task:

```
 M .env.example
 M data/paper-runtime.log
 M docs/ai-native-remediation/RUNTIME_ACCEPTANCE_PLAN.md
 M frontend/src/App.test.tsx
 M frontend/src/App.tsx
 M scripts/deepseek-keychain.sh
 M src/crypto_trader/api/app.py
 M src/crypto_trader/api/deps.py
 M src/crypto_trader/runtime/bootstrap.py
?? .ops/
?? data/crypto_trader.db.bak-0911-1547
?? data/crypto_trader.db.pre-0040
?? data/crypto_trader.db.presep8-history
?? migrations/versions/0040_runtime_settings.py
?? src/crypto_trader/llm_chief/model_control.py
?? tests/llm_chief/test_model_control.py
```

## 2. Database identity inventory (independently probed)

All rows below come from `SOURCE_MANIFEST.json`, generated by
`scripts/growth_system_dry_run.py manifest` using a SQLite **backup API
snapshot from a read-only connection** (`file:...?mode=ro`,
`PRAGMA query_only=ON`). No `immutable=1` and no raw main-file copy was used.
SHA-256 values are point-in-time observations; the live fullmarket WAL is
being written by a running process, so its hash is explicitly an
`at_read_time` observation.

| Label | Absolute path | Schema revision | Factual episodes | Legacy episodes | Reviews | Daily-review rows | Decisions |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `fullmarket` (live) | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-fullmarket/data/crypto_trader.db` | `0040_runtime_settings` | 0 | 0 | 0 | 0 | 0 |
| `canonical-clean` | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-canonical-clean/data/crypto_trader.db` | `0023_opportunity_lineage` | 33 (all `PENDING`) | 0 | 0 | 2 (legacy schema, no `status` column) | 13,271 |
| `canonical` | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-canonical/data/crypto_trader.db` | `0014_trade_plans` | table absent | 0 | 0 | 0 | table absent |
| `p0p1` | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-p0p1/data/crypto_trader.db` | `0022_episode_risk_lineage` | 0 | 0 | 0 | 1 | 1,710 |
| `local-current` | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-local-current/data/crypto_trader.db` | `0027_trade_plan_decision_unique` | **table absent** | 318 | 0 | 1 | `llm_decisions` absent; `llm_usage` = 11,613 |

Additional ownership facts (same snapshots):

* `fullmarket`: `accounts_projection = (default, USDT, 200000)`;
  latest `engine_runs` row = `RUNNING / PAPER / dummy` (run
  `run_da0fc3d9f41e45ab81093ea74f63ae05`);
  `trade_episodes`, `trade_plans`, `fills`, `orders`, `llm_decisions` all 0.
  The applied revision `0040_runtime_settings` corresponds to the
  **untracked draft migration** authored by the other task
  (`migrations/versions/0040_runtime_settings.py`). This is a cross-task DB
  state, not a verified production migration.
* `canonical-clean`: `(default, USDT)`, latest `engine_runs` row
  `STOPPED / PAPER / live_llm` (`run_658924ea...`, ended 2026-09-11
  04:05:57 UTC); `runtime_leases` row expired (`expires_at = 0.0`); the 33
  factual episodes span `closed_at` 2026-09-09 09:52:57 … 2026-09-10
  08:29:35 UTC, direction LONG=14 / SHORT=19, duplicate `trade_plan_id`=0;
  `ai_trade_reviews` = 0.
* `local-current`: has 318 legacy `ai_trade_episodes`, 419
  `trade_memory_records`, 663 fills, 2,905 trade plans, 11,613 `llm_usage`
  rows, 113,881 `tool_invocations`, 26,115 `decision_evidence` rows and no
  `trade_episodes` table at its schema revision.

## 3. Running-process association

Evidence captured with `ps` and `lsof` at 2026-09-11 ~08:41 UTC:

| Field | Observed value |
| --- | --- |
| PID | `8845` (parent PID 1) |
| Command | `crypto_trader.runtime.local_runner --host 127.0.0.1 --port 8000` |
| `cwd` | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-fullmarket` |
| Interpreter/venv | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-local-current/.venv/bin/python` (Python 3.12) |
| `PYTHONPATH` | `src` → resolves relative to cwd, i.e. **fullmarket source tree** |
| `RUNNING_SHA` env | `d6982b7` (**stale ancestor**, not the worktree HEAD `df55b11`) |
| `AUTO_START_RUNTIME` env | `false` |
| Database file held (fd) | `/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-fullmarket/data/crypto_trader.db` (SHM also held) |
| Engine mode / strategy | `PAPER` / `dummy` |

Conclusion: process ownership of the database is **proven** (open file
descriptor), but the exact source revision being executed is **UNKNOWN /
AMBIGUOUS** because the worktree HEAD (`df55b11`), the claimed
`RUNNING_SHA=d6982b7` and the venv location (`local-current`, HEAD `3bb7eea`)
all disagree. They must never be treated as one identity. The database
belongs to fullmarket's path while the interpreter belongs to
local-current — the "worktree SHA == process SHA == DB owner" assumption is
false in this deployment.

No process holds the `canonical-clean`, `local-current`, `p0p1` or
`canonical` database files. PID 738 runs `crypto_trader.market_history` in the
`canonical` worktree and holds only `data/market-data/okx.sqlite3`.

## 4. Why Review count is zero

The following candidates were checked explicitly rather than summarized as
"the model did not learn".

| Candidate | Verdict | Evidence |
| --- | --- | --- |
| Review scheduler not constructed by the live process | **STATIC INFERENCE for PID 8845 (not runtime-observed in this task)** | `runtime/bootstrap.py` builds `DailyReviewScheduler(...)` only inside `if settings.auto_start_runtime`; the process env has `AUTO_START_RUNTIME=false`, and `TradingEngine.start` creates the `daily-review` task only when `daily_review_scheduler is not None`. The same gate makes the executable strategy `DummyStrategy`, matching `engine_runs.strategy_id = dummy`.

> R15 evidence class: the scheduler/boot log line is a **static code +
> environment inference**, not a live runtime observation.  It is retained as
> inference, not upgraded to `CONFIRMED` runtime fact. |
| Backfill/recovery not run | **STATIC INFERENCE for PID 8845 (not runtime-observed in this task)** | `_recover_missed_daily_reviews()` returns immediately when the scheduler is `None`; it is called during engine start. |
| Wrong database / split-brain DB ownership | **CONFIRMED** | The live process holds the empty `fullmarket` DB (0 episodes, 0 decisions). The 33 factual episodes and 13,271 decisions are in `canonical-clean`, which no process holds. |
| Review date not yet due | **PARTLY RULED OUT** | Episodes closed on 2026-09-09 and 2026-09-10 UTC. At process start (2026-09-11 08:08 UTC) the complete previous UTC day was 2026-09-10, so at least that day's episodes were already eligible. UTC 00:00 of 2026-09-11 had passed. |
| Factual/ownership filter dropped episodes | **NOT THE BLOCKER in canonical-clean** | 33 `factual=1` rows exist with `review_status=PENDING`; the live DB simply has no rows. Whether a `build_for_closed_plan` fail-closed reason (`overlapping lifecycle`, `FUNDING_COVERAGE_UNKNOWN`, etc.) suppressed additional closed plans remains **UNKNOWN** and needs per-plan reason logging. |
| Claim stuck | **UNKNOWN** | The canonical-clean `daily_review_runs` table predates claim columns (schema 0023); it has no `status`, `claim_token`, `owner` or `claim_deadline_at`, so no claim diagnosis is possible from that snapshot. |
| Publish failure / early mark | **PARTIAL** | `ai_trade_reviews = 0` and all 33 episodes remain `PENDING`, so no review was published and no mark happened. The two `daily_review_runs` rows have `trade_count=0`, `win_rate=0`, `profit_factor=999` — evidence of the semantics bug in G01 and of an empty stats path, not of a failed LLM call. |
| Late revision/backfill gap | **UNKNOWN** | No durable stage records exist in the legacy schema; this is what G03 adds. |

The two legacy `daily_review_runs` rows are:

```
review_date=2026-09-10  trade_count=0  win_rate=0  profit_factor=999  created_at=2026-09-09 16:00:00 UTC
review_date=2026-09-11  trade_count=0  win_rate=0  profit_factor=999  created_at=2026-09-10 16:16:40 UTC
```

`profit_factor=999` is not a ratio and must not be read as a health signal.
The rows were written by the legacy schema that stored no source revision,
profile version or episode count, so their provenance is under-specified.

## 5. User-provided numeric clues — independent verification

The clue counts were treated as leads only. Cross-database addition and
`decisions × 2` billing arithmetic were not used.

| Clue | Independent finding | Status |
| --- | --- | --- |
| 318 Episodes | `local-current.ai_trade_episodes = 318` (legacy episode table) | **VERIFIED as a table count**, not as factual episodes |
| 33 Episodes | `canonical-clean.trade_episodes = 33`, all `factual=1`, all `PENDING` | **VERIFIED** |
| 13,271 decisions | `canonical-clean.llm_decisions = 13,271` | **VERIFIED** |
| 772 rows | not reproduced as any single table count in the five inspected snapshots | **NOT_VERIFIED** — needs the clue's table/query definition |
| 33,081 provider requests | not reproduced as a single table count. Observed candidates: `local-current.llm_usage=11,613`, `local-current.decision_evidence=26,115`, `local-current.tool_invocations=113,881`, `canonical-clean.llm_decisions=13,271` | **UNKNOWN** — a provider request must be defined (attempt vs successful call vs tool selection vs health probe) before any total is claimed |

## 6. What G00 explicitly does not claim

* No statement that any process is “running `98e4b6c`”, “running `df55b11`”
  or that the running process belongs to `canonical-clean`.
* No statement that the data was migrated, deployed or accepted.
* No production write, no scheduler start, no claim acquisition, no
  backfill of production reviews and no credential or environment dump.
* No pattern/lesson was promoted from any reviewed knowledge because there
  are zero reviews to promote (`ai_trade_reviews = 0`).

## 7. Reproduction

```bash
python scripts/growth_system_dry_run.py manifest \
  --db fullmarket=/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-fullmarket/data/crypto_trader.db \
  --db canonical-clean=/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-canonical-clean/data/crypto_trader.db \
  --db p0p1=/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-p0p1/data/crypto_trader.db \
  --db local-current=/Users/huhongjie/Documents/ChatGPT/crypto-auto-trading-system-local-current/data/crypto_trader.db \
  --out docs/growth-system/SOURCE_MANIFEST.json
```

Process evidence commands (read-only):

```bash
lsof -p 8845 | grep -E 'cwd|crypto_trader.db'
ps eww -p 8845 | tr ' ' '\n' | grep -E '^(RUNNING_SHA|AUTO_START_RUNTIME|PYTHONPATH|PWD)='
```
